import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flesentine.msmadrigal',
  appName: 'Ms. Madrigral',
  webDir: 'www',
};

export default config;
